<% if current_user.blog_posts.any? %>
  <ol class="post">
    <%= render current_user.blog_posts %>
    <!-- Renders as below -->
    <%#= render "posts/post", collection: current_user.blog_posts, as: :post %>
    <!-- The _post partial iterates over current_user.blog_posts to render each one -->
  </ol>
<% end %>
